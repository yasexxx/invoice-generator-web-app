# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.claude
.codex
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# Docker local secrets — create manually: mkdir secrets && echo "invoicely" > secrets/db_user.txt && echo "changeme" > secrets/db_password.txt
secrets/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
